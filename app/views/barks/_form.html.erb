<%= form_for @bark do |f| %>

  <% if @bark.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@bark.errors.count, "error") %> prohibited
        this Bark from being saved:
      </h2>
      <ul>
        <% @bark.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :title %>
    <%= f.text_field :title %><br>
  </p>

  <p>
    <%= f.label :location %>
    <%= f.text_field :location %><br>
  </p>

  <p>
    <%= f.label :content %>
    <%= f.text_area :content %><br>
  </p>

  <p>
    <%= f.submit %>
  </p>

<% end %>